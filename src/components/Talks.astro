---
import { publicTalks, publicTalksList, talkGroups } from '../data/talks';
---

<!-- Public Talks -->
<section id="presentations" class="scroll-mt-28 py-16 bg-slate-50" aria-labelledby="presentations-heading">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <h2 id="presentations-heading" class="text-2xl font-bold">Public Talks</h2>

    <!-- Featured talks with images -->
    <div class="mt-6 grid lg:grid-cols-2 gap-6">
      {publicTalks.map((talk) => (
        <article class="card rounded-2xl border border-slate-200 bg-white overflow-hidden shadow-sm">
          <img src={talk.image} alt={talk.imageAlt} class="h-48 w-full object-cover" />
          <div class="p-6">
            <h3 class="font-semibold text-lg">{talk.title}</h3>
            <p class="mt-2 text-sm text-slate-600">
              <em>{talk.eventInfo}</em>
            </p>
            {talk.videoUrl && (
              <a href={talk.videoUrl} target="_blank" rel="noopener noreferrer" class="mt-3 inline-block text-[var(--brand)] text-sm font-medium">YouTube →<span class="sr-only"> (opens in new tab)</span></a>
            )}
          </div>
        </article>
      ))}
    </div>

    <!-- Remaining talks in list -->
    <ul class="mt-10 space-y-4 list-disc list-inside text-slate-700">
      {publicTalksList.map((talk) => (
        <li>
          <span class="font-medium">{talk.type}:</span>
          {` "${talk.title}" — `}
          <em>{talk.event}</em>.
        </li>
      ))}
    </ul>
  </div>

  <div class=" mb-24">
      <p>

      </p>
  </div>
</section>

<!-- Presentations -->
<section id="talks" class="scroll-mt-28 py-16 bg-slate-50" aria-labelledby="talks-heading">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 space-y-1">
    <h2 id="talks-heading" class="text-2xl font-bold">Seminar / Colloquium / Conference Talks</h2>

    <div class="mt-6 space-y-4 text-slate-700">

      <!-- Toggle text -->
      <div class="flex justify-end mb-4">
        <button id="toggleAll" type="button" class="cursor-pointer text-[var(--brand)] text-sm font-medium" aria-expanded="true">
          Collapse All
        </button>
      </div>

      {talkGroups.map((group) => (
        <details open class="border border-slate-200 rounded-xl p-4">
          <summary class="cursor-pointer font-semibold" set:html={group.title} />
          <div class="mt-3 ml-4 space-y-1 text-sm">
            {group.rows.map((row) => (
              <div class="talk-row grid lg:grid-cols-[6rem_7rem_10rem_minmax(24rem,1fr)_1.4fr_1fr] gap-4">
                <div data-label="Date">{row.date}</div>
                <div data-label="Type">{row.type}</div>
                <div data-label="Session">{row.session}</div>
                <div data-label="Event/Inst." set:html={row.event} />
                <div data-label="City/State">{row.location}</div>
                <div data-label="Link">
                  {row.linkUrl && (
                    <a href={row.linkUrl} target="_blank" rel="noopener noreferrer" class="text-[var(--brand)] text-sm font-medium ml-2">{row.linkText || "Link →"}<span class="sr-only"> (opens in new tab)</span></a>
                  )}
                </div>
              </div>
            ))}
          </div>
        </details>
      ))}

    </div>
  </div>
</section>

<!-- Collapse/Expand script -->
<script is:inline>
  const toggleLink = document.getElementById("toggleAll");
  const allDetails = document.querySelectorAll("#talks details");
  let expanded = true;
  toggleLink?.addEventListener("click", () => {
    expanded = !expanded;
    allDetails.forEach(d => expanded ? d.setAttribute("open", "") : d.removeAttribute("open"));
    toggleLink.textContent = expanded ? "Collapse All" : "Expand All";
    toggleLink.setAttribute("aria-expanded", String(expanded));
  });
</script>

<script is:inline>
    document.addEventListener("DOMContentLoaded", () => {
        if (window.innerWidth < 1024) {
            document.querySelectorAll("#talks details[open]").forEach(d => {
                d.removeAttribute("open");
            });
            const toggleLink = document.getElementById("toggleAll");
            if (toggleLink) { toggleLink.textContent = "Expand All"; toggleLink.setAttribute("aria-expanded", "false"); }
        }
    });
</script>
